import styled from "styled-components";
import { useRouter } from "next/router";

const SideBar = () => {

    return (
        <h1>
            SideBar
        </h1>
    );
};

export default SideBar;